#!/bin/sh
# OCI poststop hook: commit container on exit 42.
# Place in /etc/containers/oci-hook.d/ or configure in containers.conf.

CONTAINER_NAME="${1}"
EXIT_CODE="${2}"

if [ "$EXIT_CODE" -eq 42 ]; then
  podman commit "$CONTAINER_NAME" localhost/self-buildah:latest
fi