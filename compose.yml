# Minimal self-building container with buildah.
services:
  self-buildah:
    image: localhost/self-buildah:latest
    pull_policy: never
    container_name: self-buildah
    environment:
      - BUILDAH_ISOLATION=chroot
    env_file:
      - path: .env
        required: false
    volumes:
      - ${BIN_DIR:-./bin}:/usr/local/bin:ro
      - ${SBIN_DIR:-./sbin}:/usr/local/sbin:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    security_opt:
      - no-new-privileges:true
    init: true
    cap_drop:
      - ALL
    cap_add:
      - SETUID
      - SETGID
      - CHOWN
      - FOWNER
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m
    deploy:
      resources:
        limits:
          memory: 1G
          cpus: '2.0'
          pids: 200
